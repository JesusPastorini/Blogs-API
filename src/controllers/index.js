const userControler = require('./user.controllers');
const categoryController = require('./category.controllers');
const postController = require('./post.controllers');

module.exports = {
    userControler,
    categoryController,
    postController,
};